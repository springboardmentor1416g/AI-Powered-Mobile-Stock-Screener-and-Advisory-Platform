# API Gateway / Backend For Frontend

This service acts as the single entry point for mobile clients.  
Implements:
- /api/v1/health
- /api/v1/metadata/stocks
- Global error handling
- Request tracing & logging
- API versioning

Run locally:
npm install
npm run dev
