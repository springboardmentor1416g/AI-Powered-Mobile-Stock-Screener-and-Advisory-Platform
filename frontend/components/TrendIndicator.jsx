export default function TrendIndicator({ value }) {
  if (value === "UP") return <span>↑</span>;
  if (value === "DOWN") return <span>↓</span>;
  return <span>→</span>;
}
export default function TrendIndicator({ value }) {
  if (value === "UP") return <span>↑</span>;
  if (value === "DOWN") return <span>↓</span>;
  return <span>→</span>;
}
